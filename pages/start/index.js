import React from "react";
import SignChoice from "../../components/SignIn/SignChoice";
import {useSelector} from "react-redux";
const index = () => {
  return <SignChoice></SignChoice>;
};

export default index;
